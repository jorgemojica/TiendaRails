<%= link_to products_path do %>
<h1>Vendelo</h1>
<% end %>

<div>
    <%= link_to 'Añadir producto', new_product_path if Current.user %>
    <%= link_to 'Mis Favoritos', favorites_path if Current.user %>
    <%= link_to 'Categorías', categories_path if Current.user&.admin? %>
    <%= link_to 'Login', new_session_path unless Current.user %>
    <%= link_to 'Sign Up', new_user_path unless Current.user %>
    <%= link_to 'Log Out', session_path(Current.user.id), data: {
        turbo_method: :delete,
        turbo_confirm: 'Are you sure?'
    } if Current.user %>
</div>
